Create a clean, modern scientific graphical abstract for a journal paper titled "Occupancy modeling using population statistics and machine learning for urban residential built environment." The image should be landscape-oriented (16:9), white background with subtle light gray (#F5F5F5) accents, and use a color palette of deep blue (#1A3A5C), warm orange (#E8703A), and light gray (#B0B8C4).

Show a left-to-right pipeline with three columns:

**Left column — INPUT (labeled "Population Statistics"):**
- Icon of a government/census document and a time-use diary book
- Labels: "CENSUS" and "TUS (Time-Use Survey)"
- Small flag/map icon for Italy (ISTAT source)

**Center column — FRAMEWORK (labeled "CENTUS Pipeline"):**
- A neural network diagram with two parallel branches labeled "LSTM" and "Transformer"
- Below the network, a small table icon labeled "Non-temporal attributes" (demographics) merging with a clock/timeline labeled "Temporal attributes" (hourly sequences)
- A funnel or merge arrow connecting the two branches into one output

**Right column — OUTPUT (labeled "Occupancy Data"):**
- A stylized apartment/residential building with three floors, each floor showing a different color-coded occupancy schedule bar (like a Gantt chart, showing day/night cycles)
- Small icons below: one house (individual), one neighborhood block (neighborhood-scale)
- Labels: "Individual" and "Neighborhood-scale"

At the top, include a subtle banner with the paper title in Inter/sans-serif font. At the bottom right, a small accuracy badge showing "~98% Accuracy" in orange. Arrows should flow left-to-right. Keep it minimalist and publication-ready — no decorative flourishes. Suitable for journal graphical abstract submission.
